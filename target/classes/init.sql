Create table receipt (id BIGINT GENERATED BY DEFAULT AS IDENTITY(START
                     WITH 1) PRIMARY KEY, reimbursement_id int, type varchar(50), price REAL);
Create table reimbursement (id BIGINT GENERATED BY DEFAULT AS IDENTITY(START
                                                 WITH 1) PRIMARY KEY, first_name varchar(50), last_name varchar(50), start_date TIMESTAMP, end_date TIMESTAMP, distance_driven REAL);
Insert into receipt (reimbursement_id, type, price) values (1, 'Nam Ha Minh', 13.95);
Insert into receipt (reimbursement_id, type, price) values (1, 'ticket', 13.95);